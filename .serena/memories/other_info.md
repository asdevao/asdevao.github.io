# Other Relevant Information

- **Testing**: No automated tests. Verify by building with `hugo` and checking for errors. Test interactively with `hugo server`.
- **Formatting**: No specific formatters. Use editor formatting for HTML/JS/YAML.
- **Linting**: None configured. Manually review code.
- **Guidelines**: 
  - Follow Hugo best practices for layouts and partials
  - Keep custom JS minimal and efficient
  - Use theme parameters for configuration where possible
  - Support mobile responsiveness
- **Design Patterns**: 
  - Event-driven JS for UI interactions
  - CSS for animations and styling
  - Hugo partials for reusable components
- **Performance**: Use lazy loading, optimize images, minimize JS
- **Accessibility**: Include ARIA labels where needed
- **Internationalization**: Use Hugo's i18n with language codes